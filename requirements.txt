# =============================================================================
# COS-PLAY (Game-AI-Agent)  —  Main environment
# Python >=3.9, <3.12 | CUDA 12.x
#
# Install:  pip install -r requirements.txt
# Or:       pip install -e .              (editable / development mode)
# Or:       bash install/install_main_env.sh  (automated conda + all deps)
#
# For the orak-mario environment (Super Mario), see install/requirements-orak-mario.txt
# =============================================================================

# ── Core ────────────────────────────────────────────────────────────────────
numpy==1.26.4
pyyaml>=6.0

# ── Deep Learning ───────────────────────────────────────────────────────────
torch>=2.1
transformers>=4.51.0
peft>=0.10
safetensors>=0.4
datasets>=2.0
accelerate>=0.30

# ── RAG ─────────────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
Pillow>=9.0

# ── Inference server ────────────────────────────────────────────────────────
vllm>=0.4
httpx>=0.24

# ── API clients (baselines & cold-start) ────────────────────────────────────
openai>=1.0
anthropic>=0.30
google-genai>=1.0

# ── Configuration ───────────────────────────────────────────────────────────
omegaconf>=2.3.0
hydra-core>=1.3.0

# ── Game environments ───────────────────────────────────────────────────────
diplomacy>=1.1.2
requests>=2.28

# ── Skill bank ──────────────────────────────────────────────────────────────
scikit-learn>=1.0

# ── Logging ─────────────────────────────────────────────────────────────────
loguru>=0.7.0
coloredlogs
tensorboard>=2.14

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=7.0

# ── Optional (uncomment if needed) ──────────────────────────────────────────
# wandb                         # experiment tracking
# lm_eval                       # academic benchmarks (MMLU-Pro, Math-500)
