# API keys and secrets - never commit
api_keys.py

# Python bytecode (can contain secrets from imported modules)
__pycache__/
*.pyc
*.pyo

# Training / co-evolution outputs (checkpoints, rollouts, logs)
runs/
